package org.example.projectproposalservice.controller;

import io.swagger.v3.oas.annotations.tags.Tag;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/admin")
@Tag(name = "Admin Management", description = "Administrative operations related to projects, proposals, and freelancers")
public class AdminController {

    // Placeholder for admin endpoints (e.g., analytics, project moderation)
}
